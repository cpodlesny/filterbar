import template from './rides-list.html';
import controller from './rides-list.controller';
import './rides-list.styl';

let ridesListComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default ridesListComponent;
