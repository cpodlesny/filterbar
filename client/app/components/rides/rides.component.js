import template from './rides.html';
import controller from './rides.controller';
import './rides.styl';

let ridesComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default ridesComponent;
